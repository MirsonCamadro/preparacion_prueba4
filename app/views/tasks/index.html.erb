<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<% @tasks.each do |task| %>
    <%= link_to task.title, task_path(id: task.id) %>
    <%= image_tag task.photo %>
    <%= link_to "Listo", todos_path, class:"btn btn-danger" %>
<% end %>